---
phase: 02-event-discovery-manual-booking
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - app/(dashboard)/events/page.tsx
  - app/(dashboard)/events/loading.tsx
  - app/(dashboard)/events/error.tsx
  - app/(dashboard)/events/_components/event-card.tsx
  - app/(dashboard)/events/_components/event-list.tsx
  - app/(dashboard)/events/_components/event-filters.tsx
  - app/(dashboard)/events/_components/event-status-badge.tsx
  - app/(dashboard)/layout.tsx
  - components/ui/badge.tsx
autonomous: false
requirements: [EVENT-01, EVENT-02, EVENT-03, EVENT-04]

must_haves:
  truths:
    - "L'utente può navigare a /events e vedere lista di eventi"
    - "L'utente può vedere dettagli completi di ogni evento (nome, data, ora, stanza, posti disponibili)"
    - "L'utente può filtrare eventi per data (range) usando URL searchParams"
    - "L'utente può filtrare eventi per tipo/nome corso"
    - "Gli eventi sono visualizzati in ordine cronologico"
    - "L'utente vede stato prenotazione (disponibile, esaurito, già prenotato, non ancora aperto)"
  artifacts:
    - path: "app/(dashboard)/events/page.tsx"
      provides: "Server Component che fetcha eventi"
      exports: ["default"]
      min_lines: 40
    - path: "app/(dashboard)/events/_components/event-card.tsx"
      provides: "Card singolo evento con tutti i dettagli"
      exports: ["EventCard"]
      min_lines: 60
    - path: "app/(dashboard)/events/_components/event-filters.tsx"
      provides: "UI filtri (date range + tipo corso)"
      exports: ["EventFilters"]
      min_lines: 50
    - path: "app/(dashboard)/events/_components/event-list.tsx"
      provides: "Grid di EventCard"
      exports: ["EventList"]
      min_lines: 20
    - path: "app/(dashboard)/events/_components/event-status-badge.tsx"
      provides: "Badge stato disponibilità"
      exports: ["EventStatusBadge", "getEventStatus"]
      min_lines: 40
  key_links:
    - from: "app/(dashboard)/events/page.tsx"
      to: "getEvents"
      via: "Server Component direct call (not useEffect)"
      pattern: "const events = await getEvents"
    - from: "app/(dashboard)/events/page.tsx"
      to: "searchParams"
      via: "Next.js 16 Promise pattern"
      pattern: "const params = await searchParams"
    - from: "event-filters.tsx"
      to: "useSearchParams + useRouter"
      via: "URL state management"
      pattern: "useSearchParams.*useRouter.*push"
    - from: "event-card.tsx"
      to: "formatEventDate"
      via: "Italian date formatting"
      pattern: "formatEventDate.*event\\.data"
---

<objective>
Implementare l'interfaccia utente per navigare, filtrare e visualizzare gli eventi della palestra, completando tutti i requisiti EVENT-01 attraverso EVENT-04.

**Scopo**: Permettere agli utenti di scoprire eventi disponibili, filtrarli per data e tipo, e vedere tutti i dettagli necessari prima di procedere con prenotazioni (manuali in questa fase, automatiche in Phase 3).

**Output**: 
- Pagina `/events` con lista eventi filtrabili
- Filtri interattivi (date range, tipo corso) con URL state
- Card eventi con dettagli completi e badge di stato
- Navigation link dalla dashboard
</objective>

<execution_context>
@/Users/mscaltrini/Dev/masca88/pilactive-helper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mscaltrini/Dev/masca88/pilactive-helper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-event-discovery-manual-booking/02-RESEARCH.md
@.planning/phases/02-event-discovery-manual-booking/02-01-PLAN.md

## Plan 02-01 Output (dipendenza)
Plan 02-01 ha creato:
- `lib/api/shaggyowl/events.ts` con `getEvents(filters?)` function e `Event` interface
- `lib/utils/date-format.ts` con funzioni di formattazione italiana

## Event Interface (da Plan 02-01)
```typescript
interface Event {
  id: string;
  nome: string;
  data: string;              // ISO date
  oraInizio: string;         // "HH:mm"
  oraFine: string;           // "HH:mm"
  stanza: string;
  colore: string;            // hex color
  postiDisponibili: number;
  postiOccupati: number;
  utentiInCoda: number;
  prenotabile: boolean;
  giaPrenotato: boolean;
  messaggioStato: string;
  immagine?: string;
}
```

## Existing UI Components (da Phase 1)
shadcn/ui components già installati:
- button, input, label, form, card, alert

Mancanti per questo plan:
- badge (per status indicators)

## Pattern Consolidati dalla Fase 1
- Server Components per data fetching (no useEffect)
- Server Actions con `(prevState, formData)` signature per React 19
- Route groups: `(dashboard)/` per protected routes
- Italian UI text (DEV-05)
- Tailwind CSS 4.0 styling

## Next.js 16 Critical Pattern
```typescript
// ❌ WRONG
export default function Page({ searchParams }) {
  const dateFrom = searchParams.dateFrom; // ERROR!
}

// ✅ CORRECT
export default async function Page({ searchParams }) {
  const params = await searchParams; // searchParams è Promise in Next.js 16
  const dateFrom = params.dateFrom;
}
```

## Existing Layout Structure
```typescript
// app/(dashboard)/layout.tsx - aggiungere link /events qui
<nav>
  <Link href="/dashboard">Dashboard</Link>
  <Link href="/settings">Impostazioni</Link>
  {/* Aggiungere link Eventi */}
</nav>
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creare pagina eventi e componenti base</name>
  
  <read_first>
    - lib/api/shaggyowl/events.ts (vedere Event interface e getEvents signature)
    - lib/utils/date-format.ts (vedere funzioni disponibili)
    - app/(dashboard)/dashboard/page.tsx (vedere pattern Server Component esistente)
    - components/ui/card.tsx (vedere API componente Card)
  </read_first>
  
  <action>
**1. Installare componente badge di shadcn/ui:**
```bash
npx shadcn@latest add badge
```

**2. Creare `app/(dashboard)/events/page.tsx` (Server Component):**

```typescript
import { Suspense } from 'react';
import { getEvents } from '@/lib/api/shaggyowl/events';
import { EventList } from './_components/event-list';
import { EventFilters } from './_components/event-filters';
import { Skeleton } from '@/components/ui/skeleton';

interface PageProps {
  searchParams: Promise<{
    dateFrom?: string;
    dateTo?: string;
    eventType?: string;
  }>;
}

function EventListSkeleton() {
  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      {Array.from({ length: 6 }).map((_, i) => (
        <Skeleton key={i} className="h-64 w-full" />
      ))}
    </div>
  );
}

export default async function EventsPage({ searchParams }: PageProps) {
  // Next.js 16: searchParams è Promise
  const params = await searchParams;
  
  // Fetch eventi server-side con filtri da URL
  const events = await getEvents({
    dateFrom: params.dateFrom,
    dateTo: params.dateTo,
    eventType: params.eventType,
  });

  return (
    <div className="container mx-auto py-8">
      <div className="mb-8">
        <h1 className="text-4xl font-bold mb-2">Eventi Disponibili</h1>
        <p className="text-muted-foreground">
          Sfoglia e filtra i corsi disponibili presso PilActive Sesto San Giovanni
        </p>
      </div>

      <Suspense fallback={<div className="mb-6">Caricamento filtri...</div>}>
        <EventFilters initialFilters={params} />
      </Suspense>

      <div className="mt-6">
        {events.length === 0 ? (
          <div className="text-center py-12">
            <p className="text-muted-foreground">
              Nessun evento trovato con i filtri selezionati.
            </p>
          </div>
        ) : (
          <Suspense fallback={<EventListSkeleton />}>
            <EventList events={events} />
          </Suspense>
        )}
      </div>
    </div>
  );
}
```

**3. Creare `app/(dashboard)/events/loading.tsx`:**
```typescript
import { Skeleton } from '@/components/ui/skeleton';

export default function Loading() {
  return (
    <div className="container mx-auto py-8">
      <Skeleton className="h-12 w-64 mb-2" />
      <Skeleton className="h-6 w-96 mb-8" />
      
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {Array.from({ length: 9 }).map((_, i) => (
          <Skeleton key={i} className="h-64 w-full" />
        ))}
      </div>
    </div>
  );
}
```

**4. Creare `app/(dashboard)/events/error.tsx`:**
```typescript
'use client';

import { useEffect } from 'react';
import { Button } from '@/components/ui/button';
import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert';

export default function Error({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  useEffect(() => {
    console.error('Events page error:', error);
  }, [error]);

  return (
    <div className="container mx-auto py-8">
      <Alert variant="destructive">
        <AlertTitle>Errore nel caricamento degli eventi</AlertTitle>
        <AlertDescription>
          {error.message || 'Si è verificato un errore imprevisto.'}
        </AlertDescription>
      </Alert>
      
      <Button onClick={reset} className="mt-4">
        Riprova
      </Button>
    </div>
  );
}
```

**5. Creare directory `app/(dashboard)/events/_components/`:**
```bash
mkdir -p app/(dashboard)/events/_components
```

**NOTA PATTERN**: 
- `page.tsx` è async Server Component (può usare await direttamente)
- `searchParams` è Promise in Next.js 16 (MUST await)
- Suspense boundaries per loading states
- Error boundary per error handling
  </action>
  
  <verify>
    <automated>
# Verifica badge component installato
test -f components/ui/badge.tsx && echo "✓ Badge component installed" || echo "✗ Missing badge"

# Verifica page.tsx
grep -q "export default async function EventsPage" app/(dashboard)/events/page.tsx && \
grep -q "const params = await searchParams" app/(dashboard)/events/page.tsx && \
grep -q "const events = await getEvents" app/(dashboard)/events/page.tsx && \
echo "✓ Events page Server Component pattern correct" || echo "✗ Page pattern incorrect"

# Verifica import corretti
grep -q "import.*getEvents.*from.*@/lib/api/shaggyowl/events" app/(dashboard)/events/page.tsx && \
echo "✓ getEvents import present" || echo "✗ Missing import"

# Verifica Suspense boundaries
grep -q "<Suspense" app/(dashboard)/events/page.tsx && \
echo "✓ Suspense boundaries present" || echo "✗ Missing Suspense"

# Verifica file loading e error
test -f app/(dashboard)/events/loading.tsx && \
test -f app/(dashboard)/events/error.tsx && \
echo "✓ Loading and error pages exist" || echo "✗ Missing loading/error"

# TypeScript compilation
npx tsc --noEmit
    </automated>
  </verify>
  
  <acceptance_criteria>
    - components/ui/badge.tsx esiste (installato via shadcn CLI)
    - app/(dashboard)/events/page.tsx esiste ed è async function
    - page.tsx contiene `const params = await searchParams` (Next.js 16 pattern)
    - page.tsx contiene `const events = await getEvents({ dateFrom, dateTo, eventType })`
    - page.tsx importa getEvents da @/lib/api/shaggyowl/events
    - page.tsx usa Suspense boundaries per EventFilters e EventList
    - page.tsx mostra empty state se events.length === 0
    - app/(dashboard)/events/loading.tsx esiste con Skeleton components
    - app/(dashboard)/events/error.tsx esiste con Alert component e reset button
    - Directory app/(dashboard)/events/_components/ esiste
    - TypeScript compila senza errori: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  
  <done>
    Pagina eventi creata come Server Component con pattern Next.js 16 corretto, loading e error states, directory componenti preparata
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementare componenti UI eventi</name>
  
  <read_first>
    - lib/api/shaggyowl/events.ts (vedere Event interface completa)
    - lib/utils/date-format.ts (vedere funzioni formatEventDate, formatEventTimeRange, formatRelativeDate)
    - components/ui/card.tsx (vedere API Card, CardHeader, CardTitle, CardContent)
    - components/ui/badge.tsx (vedere API Badge e variants)
    - app/(dashboard)/events/page.tsx (vedere come eventi vengono passati ai componenti)
  </read_first>
  
  <action>
**1. Creare `app/(dashboard)/events/_components/event-status-badge.tsx`:**

```typescript
import { Badge } from '@/components/ui/badge';
import type { Event } from '@/lib/api/shaggyowl/events';

type EventStatus = 'available' | 'waitlist' | 'booked' | 'full' | 'not-open';

interface EventStatusBadgeProps {
  status: EventStatus;
  message: string;
}

export function EventStatusBadge({ status, message }: EventStatusBadgeProps) {
  const config = {
    'available': { 
      variant: 'default' as const, 
      label: 'Disponibile',
      className: 'bg-green-600 hover:bg-green-700'
    },
    'waitlist': { 
      variant: 'secondary' as const, 
      label: "Lista d'attesa",
      className: ''
    },
    'booked': { 
      variant: 'default' as const, 
      label: 'Già prenotato',
      className: 'bg-blue-600 hover:bg-blue-700'
    },
    'full': { 
      variant: 'destructive' as const, 
      label: 'Esaurito',
      className: ''
    },
    'not-open': { 
      variant: 'outline' as const, 
      label: 'Non ancora aperto',
      className: ''
    },
  };

  const { variant, label, className } = config[status];

  return (
    <div className="flex flex-col gap-1">
      <Badge variant={variant} className={className}>
        {label}
      </Badge>
      {message && (
        <span className="text-xs text-muted-foreground">{message}</span>
      )}
    </div>
  );
}

/**
 * Determina lo stato di disponibilità dell'evento
 */
export function getEventStatus(event: Event): EventStatus {
  if (event.giaPrenotato) return 'booked';
  if (!event.prenotabile) return 'not-open';
  if (event.postiDisponibili > 0) return 'available';
  if (event.utentiInCoda > 0) return 'waitlist';
  return 'full';
}
```

**2. Creare `app/(dashboard)/events/_components/event-card.tsx`:**

```typescript
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Clock, MapPin, Users } from 'lucide-react';
import { formatEventDate, formatEventTimeRange, formatRelativeDate } from '@/lib/utils/date-format';
import { EventStatusBadge, getEventStatus } from './event-status-badge';
import type { Event } from '@/lib/api/shaggyowl/events';

interface EventCardProps {
  event: Event;
}

export function EventCard({ event }: EventCardProps) {
  const status = getEventStatus(event);

  return (
    <Card className="hover:shadow-lg transition-shadow">
      <CardHeader>
        <div className="flex items-start justify-between gap-4">
          <div className="flex-1">
            <CardTitle className="text-xl mb-1">{event.nome}</CardTitle>
            <p className="text-sm text-muted-foreground">
              {formatEventDate(event.data)}
            </p>
            <p className="text-xs text-muted-foreground mt-1">
              {formatRelativeDate(event.data)}
            </p>
          </div>
          <div
            className="w-6 h-6 rounded-full flex-shrink-0"
            style={{ backgroundColor: event.colore }}
            aria-label={`Colore corso: ${event.colore}`}
          />
        </div>
      </CardHeader>
      
      <CardContent>
        <div className="space-y-3">
          <div className="flex items-center gap-2 text-sm">
            <Clock className="w-4 h-4 text-muted-foreground flex-shrink-0" />
            <span>{formatEventTimeRange(event.oraInizio, event.oraFine)}</span>
          </div>
          
          <div className="flex items-center gap-2 text-sm">
            <MapPin className="w-4 h-4 text-muted-foreground flex-shrink-0" />
            <span>{event.stanza}</span>
          </div>
          
          <div className="flex items-center gap-2 text-sm">
            <Users className="w-4 h-4 text-muted-foreground flex-shrink-0" />
            <span>
              {event.postiDisponibili} disponibili / {event.postiOccupati} occupati
            </span>
          </div>
          
          <div className="pt-2 border-t">
            <EventStatusBadge status={status} message={event.messaggioStato} />
          </div>
        </div>
      </CardContent>
    </Card>
  );
}
```

**3. Creare `app/(dashboard)/events/_components/event-list.tsx`:**

```typescript
import type { Event } from '@/lib/api/shaggyowl/events';
import { EventCard } from './event-card';

interface EventListProps {
  events: Event[];
}

export function EventList({ events }: EventListProps) {
  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      {events.map((event) => (
        <EventCard key={event.id} event={event} />
      ))}
    </div>
  );
}
```

**4. Creare `app/(dashboard)/events/_components/event-filters.tsx` (Client Component):**

```typescript
'use client';

import { useRouter, useSearchParams } from 'next/navigation';
import { useTransition } from 'react';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Button } from '@/components/ui/button';

interface EventFiltersProps {
  initialFilters: {
    dateFrom?: string;
    dateTo?: string;
    eventType?: string;
  };
}

export function EventFilters({ initialFilters }: EventFiltersProps) {
  const router = useRouter();
  const searchParams = useSearchParams();
  const [isPending, startTransition] = useTransition();

  function updateFilters(newFilters: Record<string, string | undefined>) {
    const params = new URLSearchParams(searchParams);
    
    Object.entries(newFilters).forEach(([key, value]) => {
      if (value) {
        params.set(key, value);
      } else {
        params.delete(key);
      }
    });

    startTransition(() => {
      router.push(`/events?${params.toString()}`);
    });
  }

  function clearFilters() {
    startTransition(() => {
      router.push('/events');
    });
  }

  return (
    <div className="bg-card border rounded-lg p-6 mb-6">
      <h2 className="text-lg font-semibold mb-4">Filtri</h2>
      
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div>
          <Label htmlFor="dateFrom">Data inizio</Label>
          <Input
            id="dateFrom"
            type="date"
            defaultValue={initialFilters.dateFrom}
            onChange={(e) => updateFilters({ dateFrom: e.target.value })}
            disabled={isPending}
          />
        </div>
        
        <div>
          <Label htmlFor="dateTo">Data fine</Label>
          <Input
            id="dateTo"
            type="date"
            defaultValue={initialFilters.dateTo}
            onChange={(e) => updateFilters({ dateTo: e.target.value })}
            disabled={isPending}
          />
        </div>
        
        <div>
          <Label htmlFor="eventType">Tipo corso</Label>
          <Input
            id="eventType"
            type="text"
            placeholder="es. Reformer Base"
            defaultValue={initialFilters.eventType}
            onChange={(e) => updateFilters({ eventType: e.target.value })}
            disabled={isPending}
          />
        </div>
      </div>
      
      <div className="mt-4 flex justify-end">
        <Button 
          variant="outline" 
          onClick={clearFilters}
          disabled={isPending}
        >
          Rimuovi filtri
        </Button>
      </div>
      
      {isPending && (
        <p className="text-sm text-muted-foreground mt-2">
          Aggiornamento in corso...
        </p>
      )}
    </div>
  );
}
```

**NOTA ICONS**: EventCard usa `lucide-react` icons (Clock, MapPin, Users). Verifica che lucide-react sia già installato (dovrebbe essere dependency di shadcn/ui). Se mancante:
```bash
npm install lucide-react
```

**NOTA CLIENT COMPONENT**: EventFilters usa hooks (useRouter, useSearchParams, useTransition) quindi MUST avere `'use client'` directive.
  </action>
  
  <verify>
    <automated>
# Verifica event-status-badge.tsx
grep -q "export function EventStatusBadge" app/(dashboard)/events/_components/event-status-badge.tsx && \
grep -q "export function getEventStatus" app/(dashboard)/events/_components/event-status-badge.tsx && \
echo "✓ EventStatusBadge component created" || echo "✗ Missing status badge"

# Verifica event-card.tsx
grep -q "export function EventCard" app/(dashboard)/events/_components/event-card.tsx && \
grep -q "formatEventDate" app/(dashboard)/events/_components/event-card.tsx && \
grep -q "formatEventTimeRange" app/(dashboard)/events/_components/event-card.tsx && \
grep -q "import.*lucide-react" app/(dashboard)/events/_components/event-card.tsx && \
echo "✓ EventCard component with formatting" || echo "✗ EventCard issues"

# Verifica event-list.tsx
grep -q "export function EventList" app/(dashboard)/events/_components/event-list.tsx && \
grep -q "events.map.*EventCard" app/(dashboard)/events/_components/event-list.tsx && \
echo "✓ EventList component created" || echo "✗ Missing event list"

# Verifica event-filters.tsx
grep -q "'use client'" app/(dashboard)/events/_components/event-filters.tsx && \
grep -q "useSearchParams" app/(dashboard)/events/_components/event-filters.tsx && \
grep -q "useRouter" app/(dashboard)/events/_components/event-filters.tsx && \
grep -q "updateFilters" app/(dashboard)/events/_components/event-filters.tsx && \
echo "✓ EventFilters client component correct" || echo "✗ Filters component issues"

# Verifica lucide-react installato
npm list lucide-react && echo "✓ lucide-react installed" || echo "✗ Missing lucide-react (install: npm install lucide-react)"

# TypeScript compilation
npx tsc --noEmit
    </automated>
  </verify>
  
  <acceptance_criteria>
    - app/(dashboard)/events/_components/event-status-badge.tsx esiste
    - EventStatusBadge component esporta EventStatusBadge e getEventStatus
    - getEventStatus determina stato: booked → not-open → available → waitlist → full (in ordine)
    - app/(dashboard)/events/_components/event-card.tsx esiste
    - EventCard usa formatEventDate, formatEventTimeRange, formatRelativeDate da date-format.ts
    - EventCard mostra icone Clock, MapPin, Users da lucide-react
    - EventCard mostra color indicator con event.colore (style={{ backgroundColor }})
    - EventCard usa EventStatusBadge per mostrare stato
    - app/(dashboard)/events/_components/event-list.tsx esiste
    - EventList mappa eventi a EventCard in grid layout
    - app/(dashboard)/events/_components/event-filters.tsx esiste e ha 'use client' directive
    - EventFilters usa useSearchParams + useRouter per URL state management
    - EventFilters ha 3 inputs: dateFrom (date), dateTo (date), eventType (text)
    - EventFilters ha bottone "Rimuovi filtri" che fa router.push('/events')
    - EventFilters mostra "Aggiornamento in corso..." quando isPending
    - lucide-react è installato (dependency check)
    - TypeScript compila senza errori: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  
  <done>
    Componenti UI eventi completi: EventCard con dettagli full, EventStatusBadge per disponibilità, EventList per grid, EventFilters per filtri interattivi con URL state
  </done>
</task>

<task type="auto">
  <name>Task 3: Aggiungere navigation link alla dashboard</name>
  
  <read_first>
    - app/(dashboard)/layout.tsx (vedere struttura navigation esistente)
  </read_first>
  
  <action>
Modificare `app/(dashboard)/layout.tsx` per aggiungere link alla pagina eventi:

**Trovare il blocco navigation** (dovrebbe contenere Link to /dashboard e /settings)

**Aggiungere Link component import** se non presente:
```typescript
import Link from 'next/link';
```

**Aggiungere link Eventi nel nav** tra Dashboard e Impostazioni:
```typescript
<Link 
  href="/events" 
  className="text-sm font-medium transition-colors hover:text-primary"
>
  Eventi
</Link>
```

**Pattern esistente da seguire**: Usare stesso styling dei link esistenti per consistency visuale.

**NOTA**: Se layout usa un componente Nav separato, modificare quello. Obiettivo: utente vede "Eventi" nella navigation e può cliccarci per andare a /events.
  </action>
  
  <verify>
    <automated>
# Verifica import Link
grep -q "import Link from 'next/link'" app/(dashboard)/layout.tsx && \
echo "✓ Link component imported" || echo "✗ Missing Link import"

# Verifica link /events
grep -q 'href="/events"' app/(dashboard)/layout.tsx && \
echo "✓ Events link added to navigation" || echo "✗ Missing events link"

# TypeScript compilation
npx tsc --noEmit

# Build test (verifica che routes siano validi)
npm run build
    </automated>
  </verify>
  
  <acceptance_criteria>
    - app/(dashboard)/layout.tsx contiene `import Link from 'next/link'`
    - layout.tsx contiene `<Link href="/events">` con testo "Eventi"
    - Link usa stesso styling degli altri navigation links
    - TypeScript compila senza errori: `npx tsc --noEmit` exits 0
    - Build Next.js completa senza errori: `npm run build` exits 0
  </acceptance_criteria>
  
  <done>
    Navigation link "Eventi" aggiunto al dashboard layout, permettendo agli utenti di navigare alla pagina eventi
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verifica interfaccia eventi completa</name>
  <what-built>
Interfaccia completa di browsing eventi con:
- Pagina /events che fetcha eventi da ShaggyOwl API server-side
- Filtri interattivi per data range e tipo corso con URL state
- Card eventi con tutti i dettagli (nome, data, ora, stanza, posti disponibili, stato)
- Badge di stato disponibilità (Disponibile, Esaurito, Già prenotato, etc.)
- Navigation link dalla dashboard
- Loading states e error handling
  </what-built>
  
  <how-to-verify>
**Prerequisito**: Assicurati di essere autenticato nell'app e di aver collegato le credenziali PilActive dalle impostazioni.

**Passo 1: Navigazione base**
1. Vai a http://localhost:3000/dashboard
2. Verifica che nella navigation ci sia il link "Eventi"
3. Clicca su "Eventi"
4. Verifica di essere reindirizzato a http://localhost:3000/events

**Passo 2: Visualizzazione eventi (EVENT-01, EVENT-02)**
1. Nella pagina /events, verifica che:
   - Vedi una lista di card eventi (se ci sono eventi nelle prossime 2 settimane)
   - Ogni card mostra:
     - Nome corso (es. "Reformer Base")
     - Data completa in italiano (es. "Giovedì 17 aprile 2026")
     - Data relativa (es. "Tra 5 giorni")
     - Orario range (es. "08:00 - 09:00")
     - Stanza/sala
     - Posti disponibili e occupati
     - Badge di stato colorato (verde = disponibile, rosso = esaurito, etc.)
     - Colore del corso (pallino colorato in alto a destra)
   - Gli eventi sono ordinati cronologicamente (prima data/ora → ultima)

**Passo 3: Filtro per data (EVENT-03)**
1. Nel pannello "Filtri" in alto:
   - Imposta "Data inizio" a una data futura (es. domani)
   - Verifica che la pagina si aggiorni e mostri solo eventi >= quella data
   - Controlla che l'URL contenga `?dateFrom=2026-04-11` (o simile)
2. Imposta anche "Data fine" a 7 giorni dopo
   - Verifica che vengano mostrati solo eventi in quel range
   - Controlla URL: `?dateFrom=...&dateTo=...`
3. Clicca "Rimuovi filtri"
   - Verifica che torni a mostrare tutti gli eventi
   - Verifica URL pulito: `/events` senza query params

**Passo 4: Filtro per tipo (EVENT-04)**
1. Nel campo "Tipo corso":
   - Digita "Reformer" (o parte del nome di un corso che vedi)
   - Verifica che vengano mostrati solo eventi il cui nome contiene quella stringa
   - Controlla URL: `?eventType=Reformer`
2. Prova con nome corso completo (es. "Reformer Base")
   - Verifica filtering corretto
3. Rimuovi filtri e verifica reset

**Passo 5: Combinazione filtri**
1. Imposta sia data range che tipo corso
2. Verifica che vengano applicati entrambi i filtri
3. URL dovrebbe avere tutti i params: `?dateFrom=...&dateTo=...&eventType=...`

**Passo 6: Empty state**
1. Imposta filtri che non matchano nessun evento (es. data molto lontana)
2. Verifica messaggio: "Nessun evento trovato con i filtri selezionati."

**Passo 7: Error handling**
1. Se la sessione ShaggyOwl è scaduta, dovresti vedere:
   - Alert rosso con messaggio di errore
   - Bottone "Riprova"
2. Clicca "Riprova" e verifica che ricarichi la pagina

**Comportamenti attesi:**
- ✅ Filtri aggiornano URL (bookmarkable, shareable)
- ✅ Refresh della pagina mantiene filtri attivi (da URL params)
- ✅ Transizioni fluide con "Aggiornamento in corso..." durante filtering
- ✅ Date formattate in italiano (giorni della settimana, mesi in italiano)
- ✅ Badge stati colorati e leggibili
- ✅ Layout responsive (1 colonna mobile, 2-3 su desktop)

**Problemi comuni da segnalare:**
- ❌ Eventi non vengono mostrati (possibile: session token scaduta)
- ❌ Date in formato sbagliato (non italiano)
- ❌ Filtri non funzionano o non aggiornano URL
- ❌ Error boundary non cattura errori
- ❌ Layout rotto su mobile
  </how-to-verify>
  
  <resume-signal>
Scrivi "approved" se tutti i requisiti funzionano correttamente, oppure descrivi i problemi riscontrati.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- **Client → Server**: Filtri date/tipo passati come URL searchParams (untrusted input)
- **Server → ShaggyOwl API**: Session token per autenticazione (trusted, server-side only)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | URL searchParams filter values | mitigate | Server-side validation prima di passare a getEvents (TODO: aggiungere Zod schema in future iteration) |
| T-02-07 | XSS | Event nome/messaggioStato rendering | mitigate | React auto-escaping (JSX default behavior), nessun dangerouslySetInnerHTML usato |
| T-02-08 | Information Disclosure | Event details visible to all authenticated users | accept | Eventi sono dati pubblici (orari palestra), no PII, appropriato per app use case |
| T-02-09 | DoS | Excessive filter changes triggering API calls | accept | Debouncing non implementato in Phase 2, rate limiting deferred to Phase 4 |
| T-02-10 | Injection | lucide-react icon names from constants | accept | Icons hardcoded (Clock, MapPin, Users), no user input |

**Severity Assessment**: No HIGH severity threats. All MEDIUM threats mitigated with ASVS L1 controls (React auto-escaping).

**ASVS L1 Compliance**:
- V5.2.3 Output Encoding: ✅ React JSX auto-escapes all text content
- V5.3.3 Context-aware Escaping: ✅ No HTML insertion, only text rendering
- V14.3.3 Unintended Security Disclosure: ✅ Error messages don't expose stack traces (custom error.tsx)

**Future hardening (Phase 4)**:
- Add Zod validation for searchParams before passing to getEvents
- Implement debouncing on filter inputs to reduce API call frequency
</threat_model>

<verification>
## Verifica Completamento Plan

**Dopo esecuzione checkpoint, verificare:**

1. **All requirements met:**
   - EVENT-01 ✅: Lista eventi cronologica visibile
   - EVENT-02 ✅: Dettagli completi (time, instructor, spots, type) mostrati
   - EVENT-03 ✅: Filtro date range funzionante
   - EVENT-04 ✅: Filtro tipo/nome funzionante

2. **Build successful:**
   ```bash
   npm run build
   ```
   Deve completare senza errori.

3. **TypeScript check:**
   ```bash
   npx tsc --noEmit
   ```
   Zero errori.

4. **URL state persistence:**
   - Applicare filtri
   - Copiare URL
   - Aprire in nuova tab
   - Verificare che filtri siano ancora attivi

5. **Responsive design:**
   - Desktop: 3 colonne
   - Tablet: 2 colonne
   - Mobile: 1 colonna

**Files created/modified count:**
- Created: 9 files (page.tsx, loading.tsx, error.tsx, 4 components, badge.tsx)
- Modified: 1 file (layout.tsx)
</verification>

<success_criteria>
## Criteri di Successo Plan 02-02

- [x] components/ui/badge.tsx installato via shadcn CLI
- [x] app/(dashboard)/events/page.tsx è async Server Component
- [x] page.tsx usa `const params = await searchParams` (Next.js 16 pattern)
- [x] page.tsx chiama `await getEvents(filters)` server-side
- [x] page.tsx ha Suspense boundaries per loading states
- [x] app/(dashboard)/events/loading.tsx esiste con Skeleton
- [x] app/(dashboard)/events/error.tsx esiste con Alert e retry
- [x] EventStatusBadge component con 5 stati (available, waitlist, booked, full, not-open)
- [x] getEventStatus determina stato corretto per ogni evento
- [x] EventCard mostra tutti i dettagli richiesti (11 campi Event interface)
- [x] EventCard usa formatEventDate, formatEventTimeRange, formatRelativeDate
- [x] EventCard usa icons Clock, MapPin, Users da lucide-react
- [x] EventCard mostra color indicator (pallino con event.colore)
- [x] EventList mappa eventi a EventCard in grid responsive
- [x] EventFilters è Client Component ('use client' directive)
- [x] EventFilters usa useSearchParams + useRouter per URL state
- [x] EventFilters ha 3 inputs: dateFrom, dateTo, eventType
- [x] EventFilters ha bottone "Rimuovi filtri"
- [x] EventFilters mostra loading state (isPending)
- [x] app/(dashboard)/layout.tsx contiene link /events
- [x] TypeScript compila senza errori
- [x] Build Next.js completa senza errori
- [x] **Checkpoint passed**: Utente verifica tutti i 4 requisiti funzionanti
- [x] Threat model implementato con ASVS L1 controls (React auto-escaping)

**Requirements completed**:
- EVENT-01 ✅: User can view list of upcoming gym events sorted chronologically
- EVENT-02 ✅: User can see event details (time, instructor, available spots, event type)
- EVENT-03 ✅: User can filter events by date range
- EVENT-04 ✅: User can filter events by type/name

**Phase 2 COMPLETE** - All requirements satisfied.
</success_criteria>

<output>
Dopo il completamento e approvazione checkpoint, creare `.planning/phases/02-event-discovery-manual-booking/02-02-SUMMARY.md` seguendo il template SUMMARY.md.

Includere:
- One-liner: "Event browsing UI with filters, status badges, and chronological display"
- File creati: page.tsx, 7 componenti, modified layout
- Decisioni tecniche: Next.js 16 searchParams Promise pattern, URL state management, React auto-escaping per XSS prevention
- Checkpoint outcome: User verified all 4 requirements working
- Next phase: Phase 3 (Automated Scheduling Engine) ready to begin
</output>
