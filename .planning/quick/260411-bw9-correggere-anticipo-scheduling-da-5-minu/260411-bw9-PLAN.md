---
phase: quick-260411-bw9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/utils/booking-calculator.ts
  - app/actions/scheduled-bookings.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Il sistema schedula le prenotazioni 10 secondi prima del momento esatto (non 5 minuti)"
    - "Il nome della costante riflette l'unità di misura in secondi"
  artifacts:
    - path: "lib/utils/booking-calculator.ts"
      provides: "DEFAULT_ADVANCE_SECONDS constant set to 10"
      exports: ["DEFAULT_ADVANCE_SECONDS"]
    - path: "app/actions/scheduled-bookings.ts"
      provides: "Uses DEFAULT_ADVANCE_SECONDS with seconds-to-minutes conversion"
      contains: "DEFAULT_ADVANCE_SECONDS / 60"
  key_links:
    - from: "app/actions/scheduled-bookings.ts"
      to: "lib/utils/booking-calculator.ts"
      via: "Import DEFAULT_ADVANCE_SECONDS instead of DEFAULT_ADVANCE_MINUTES"
      pattern: "import.*DEFAULT_ADVANCE_SECONDS"
---

<objective>
Correggere il timing di anticipo dello scheduling automatico da 5 minuti a 10 secondi.

Purpose: Ridurre l'anticipo della schedulazione per avvicinarsi il più possibile al momento esatto di apertura della finestra di prenotazione, minimizzando il rischio di tentativi prematuri mantenendo un margine di sicurezza minimo.

Output: Costante rinominata da `DEFAULT_ADVANCE_MINUTES` a `DEFAULT_ADVANCE_SECONDS` con valore 10, e logica aggiornata per convertire secondi in minuti quando necessario.
</objective>

<execution_context>
@/Users/mscaltrini/Dev/masca88/pilactive-helper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mscaltrini/Dev/masca88/pilactive-helper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mscaltrini/Dev/masca88/pilactive-helper/.planning/STATE.md
@/Users/mscaltrini/Dev/masca88/pilactive-helper/lib/utils/booking-calculator.ts
@/Users/mscaltrini/Dev/masca88/pilactive-helper/app/actions/scheduled-bookings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rinominare e modificare la costante di anticipo da minuti a secondi</name>
  <files>
    lib/utils/booking-calculator.ts
    app/actions/scheduled-bookings.ts
  </files>
  <action>
**In `lib/utils/booking-calculator.ts`:**

1. Rinominare `DEFAULT_ADVANCE_MINUTES` in `DEFAULT_ADVANCE_SECONDS`
2. Cambiare il valore da `5` a `10`
3. Aggiornare il JSDoc comment per riflettere che l'unità è ora in secondi
4. Aggiornare la firma della funzione `calculateBookingTime` per accettare `advanceMinutes` (manteniamo minuti come parametro interno perché Temporal API usa minuti)

**In `app/actions/scheduled-bookings.ts`:**

1. Aggiornare l'import: `import { calculateBookingTime, DEFAULT_ADVANCE_SECONDS } from "@/lib/utils/booking-calculator"`
2. Modificare la chiamata a `calculateBookingTime`: passare `DEFAULT_ADVANCE_SECONDS / 60` per convertire secondi in minuti
3. Aggiornare il commento alla riga 63 per riflettere "10 secondi" invece di "5 minuti"

**Rationale:** Manteniamo `advanceMinutes` come parametro della funzione perché l'API Temporal usa minuti (può accettare frazioni). Convertiamo secondi in minuti al punto di chiamata per chiarezza.
  </action>
  <verify>
    <automated>npm run build && grep -q "DEFAULT_ADVANCE_SECONDS = 10" lib/utils/booking-calculator.ts && grep -q "DEFAULT_ADVANCE_SECONDS / 60" app/actions/scheduled-bookings.ts</automated>
  </verify>
  <done>
- ✅ `DEFAULT_ADVANCE_SECONDS` esportato da booking-calculator.ts con valore 10
- ✅ JSDoc aggiornato per riflettere secondi invece di minuti
- ✅ scheduled-bookings.ts importa `DEFAULT_ADVANCE_SECONDS`
- ✅ Chiamata a `calculateBookingTime` usa `DEFAULT_ADVANCE_SECONDS / 60`
- ✅ Commenti aggiornati per indicare "10 secondi"
- ✅ Build TypeScript passa senza errori
  </done>
</task>

</tasks>

<verification>
**Verifica manuale (opzionale):**

1. Controllare che l'import in `scheduled-bookings.ts` sia corretto
2. Verificare che non ci siano riferimenti residui a `DEFAULT_ADVANCE_MINUTES` nel codebase
3. Confermare che il calcolo `DEFAULT_ADVANCE_SECONDS / 60` produce `0.16666...` minuti (10 secondi)

**Verifica automatica:**
```bash
npm run build
grep -r "DEFAULT_ADVANCE_MINUTES" --include="*.ts" --include="*.tsx" app/ lib/
# Dovrebbe trovare solo nei file di documentazione/planning, non nel codice
```
</verification>

<success_criteria>
1. La costante è rinominata da `DEFAULT_ADVANCE_MINUTES` a `DEFAULT_ADVANCE_SECONDS`
2. Il valore è cambiato da `5` a `10`
3. Il codice converte correttamente secondi in minuti quando chiama `calculateBookingTime`
4. La build TypeScript passa senza errori
5. Non ci sono riferimenti residui a `DEFAULT_ADVANCE_MINUTES` nel codice di produzione
6. I commenti e la documentazione riflettono il nuovo timing di 10 secondi
</success_criteria>

<output>
After completion, create `.planning/quick/260411-bw9-correggere-anticipo-scheduling-da-5-minu/260411-bw9-SUMMARY.md`
</output>
