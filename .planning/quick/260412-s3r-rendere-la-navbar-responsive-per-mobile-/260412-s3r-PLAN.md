---
phase: quick-260412-s3r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/layout.tsx
  - components/ui/sheet.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Su desktop i link di navigazione sono visibili normalmente"
    - "Su mobile (<768px) i link sono nascosti e appare un'icona hamburger"
    - "Cliccando l'hamburger si apre un drawer laterale con i link"
    - "Il drawer si chiude dopo aver selezionato un link"
  artifacts:
    - path: "components/ui/sheet.tsx"
      provides: "Sheet component da shadcn/ui per mobile drawer"
      exports: ["Sheet", "SheetTrigger", "SheetContent", "SheetHeader", "SheetTitle"]
    - path: "app/(dashboard)/layout.tsx"
      provides: "Navbar responsive con menu hamburger mobile"
      min_lines: 80
  key_links:
    - from: "app/(dashboard)/layout.tsx"
      to: "components/ui/sheet.tsx"
      via: "import Sheet components"
      pattern: "import.*Sheet.*from.*@/components/ui/sheet"
    - from: "Sheet mobile menu"
      to: "navigation links"
      via: "Tailwind responsive classes (md:hidden, md:flex)"
      pattern: "className=.*md:(hidden|flex)"
---

<objective>
Rendere la navbar responsive per dispositivi mobile utilizzando un menu hamburger che apre un drawer laterale con i link di navigazione.

Purpose: Migliorare l'usabilità su dispositivi mobili nascondendo i link di navigazione su schermi piccoli e fornendo un menu hamburger accessibile.
Output: Navbar responsive con menu hamburger mobile usando shadcn/ui Sheet component.
</objective>

<execution_context>
@/Users/mscaltrini/Dev/masca88/pilactive-helper/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mscaltrini/Dev/masca88/pilactive-helper/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Current navbar is in `app/(dashboard)/layout.tsx` with inline navigation links (lines 25-50).

Stack: Next.js 16 + React 19 + Tailwind CSS 4.0 + shadcn/ui (Base UI components).

Existing shadcn/ui components available: Button, Card, Form, Input, Label, Popover, Badge, Alert, Calendar, Skeleton.

Need to add Sheet component from shadcn/ui for mobile drawer functionality.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sheet component da shadcn/ui</name>
  <files>components/ui/sheet.tsx</files>
  <action>
Creare il file `components/ui/sheet.tsx` copiando il Sheet component da shadcn/ui CLI v4.

Il component Sheet è basato su Base UI (come gli altri component già presenti nel progetto) e fornisce Dialog, DialogBackdrop, DialogPanel per creare drawer laterali.

Implementare:
- Sheet root component (wrapper per dialog)
- SheetTrigger (pulsante che apre il drawer)
- SheetContent (contenuto del drawer con animazioni slide-in da sinistra)
- SheetHeader, SheetTitle per intestazione del drawer
- SheetDescription (opzionale)
- SheetClose per chiudere il drawer

Utilizzare le stesse convenzioni degli altri component ui già presenti (Base UI + Tailwind + class-variance-authority per variants).

Animazioni: slide-in from left con transizione smooth (duration-200).
  </action>
  <verify>
    <automated>npx tsc --noEmit components/ui/sheet.tsx</automated>
  </verify>
  <done>
File components/ui/sheet.tsx esiste, esporta Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle, nessun errore TypeScript.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rendere navbar responsive con hamburger menu</name>
  <files>app/(dashboard)/layout.tsx</files>
  <action>
Modificare `app/(dashboard)/layout.tsx` per rendere la navbar responsive:

**Desktop (≥768px):**
- Logo PilActive Helper a sinistra
- Link di navigazione orizzontali (Dashboard, Eventi, Prenotazioni, Impostazioni) visibili normalmente
- Email utente + LogoutButton a destra

**Mobile (<768px):**
- Logo a sinistra
- Hamburger icon (Menu da lucide-react) a sinistra del logo
- Link di navigazione NASCOSTI (hidden md:flex)
- Email utente nascosta, solo LogoutButton visibile
- Hamburger apre Sheet drawer da sinistra con i link verticali

Implementazione:
1. Importare Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle da @/components/ui/sheet
2. Importare Menu icon da lucide-react
3. Wrappare tutto in Sheet component
4. Aggiungere SheetTrigger con Menu icon, visibile solo su mobile (md:hidden)
5. Desktop nav links: aggiungere `hidden md:flex` classes
6. SheetContent: lista verticale dei link (Dashboard, Eventi, Prenotazioni, Impostazioni) con gap-4
7. Ogni link nel drawer: text-base, padding adeguato, hover state
8. Email utente: nascondere su mobile con `hidden md:block`

Note:
- Mantenere tutte le classi Tailwind esistenti per desktop
- Link nel drawer devono chiudere automaticamente il Sheet dopo il click (comportamento default di Next.js Link)
- Usare italiano per tutti i testi (già presente)
  </action>
  <verify>
    <automated>npx tsc --noEmit app/(dashboard)/layout.tsx && npm run build</automated>
  </verify>
  <done>
Su mobile (<768px) hamburger menu visibile e link nascosti, su desktop (≥768px) link visibili e hamburger nascosto. Cliccando hamburger apre drawer con link verticali. Build Next.js completa senza errori.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | UI-only changes, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-s3r-01 | N/A | UI Components | accept | Pure presentational changes, no security impact. Uses existing authenticated layout. |
</threat_model>

<verification>
**Manual verification:**
1. Aprire app in browser su desktop (≥768px): link navigazione visibili, no hamburger
2. Ridimensionare a mobile (<768px): hamburger visibile, link nascosti
3. Cliccare hamburger: drawer si apre da sinistra con link verticali
4. Cliccare un link nel drawer: navigazione funziona e drawer si chiude
5. Email utente nascosta su mobile, LogoutButton sempre visibile

**Automated checks:**
- TypeScript compilation passa
- Next.js build completa senza errori
</verification>

<success_criteria>
- [ ] Sheet component aggiunto in components/ui/sheet.tsx
- [ ] Navbar in app/(dashboard)/layout.tsx usa Tailwind responsive classes
- [ ] Desktop (≥768px): link visibili normalmente, no hamburger
- [ ] Mobile (<768px): hamburger visibile, link nascosti
- [ ] Cliccando hamburger: drawer si apre con link verticali
- [ ] Link nel drawer funzionano e chiudono il drawer
- [ ] Build Next.js passa senza errori TypeScript
</success_criteria>

<output>
After completion, create `.planning/quick/260412-s3r-rendere-la-navbar-responsive-per-mobile-/260412-s3r-SUMMARY.md`
</output>
